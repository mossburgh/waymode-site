# Waymode site

The static marketing site for [waymode.ai](https://waymode.ai).

```sh
npm install
npm run check
npm run dev
```

Deploy with `npm run deploy`. Cloudflare Workers serves `public/` and manages the apex and `www` custom domains.
