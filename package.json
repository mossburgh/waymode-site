{
  "name": "waymode-site",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "node scripts/check.mjs"
  },
  "devDependencies": {
    "wrangler": "4.135.0"
  }
}
